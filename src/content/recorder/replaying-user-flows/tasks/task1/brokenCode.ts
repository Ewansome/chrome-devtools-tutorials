// Original recording step:
// { type: "click", selectors: ["button.add-to-cart"] }
// After code change, the button class changed to "btn-add"
// The step fails because "button.add-to-cart" no longer exists